# Core dependencies (already used by the project)
requests>=2.31.0
openai>=1.40.0
python-dotenv>=1.0.0
python-docx>=1.1.0

# FastAPI web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Word → HTML conversion for in-browser preview
mammoth>=1.8.0
